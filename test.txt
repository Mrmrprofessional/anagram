                $sort_word = sort(str_split($input_word));
                $sort_compare = sort(str_split($word));









                                $split_word = str_split($input_word);
                                $sort_word = sort($split_word);
                                $split_compare = str_split($word);
                                $sort_compare = sort($split_compare);
