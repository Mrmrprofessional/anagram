BEHAVIOR

It returns single-character strings in the array if they are an anagram of the initial input

It returns single-character strings in the array if they are an anagram of the initial input

It returns multi-character strings in the array if they are anagrams of the initial string input

Will return nothing if no array items match the initial input
